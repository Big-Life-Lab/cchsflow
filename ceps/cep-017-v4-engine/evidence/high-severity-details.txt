
================================================================================
## api-surface-ux — HIGH severity details

### 18 exported calculate_* functions are 'DOCUMENTATION ONLY' stubs whose only body is stop()
WHY: Roughly 14% of the public API (18/125) is fake: calling a documented, exported function with the signature it advertises throws an error. Errors are being used as a documentation channel because the package has no other way to document worksheet-routed derived variables. This violates the basic contract of an R export, confuses autocompletion-driven discovery, and would draw immediate criticism in a CRAN or JOSS-style review.
EVIDENCE: /Users/dmanuel/github/cchsflow/R/smoking-status.R:196-198 calculate_SMK_005 <- function(data, output_format = "tagged_na") { stop("DOCUMENTATION ONLY: Use rec_with_table(data, 'SMK_005') for implementation") } | grep 'stop("DOCUMENTATION ONLY' R/*.R -> 18 hits: smoke-intensity.R:209,250,286,322 (SMK_204, SMK_208, SMK_05B, SMK_05C); smoke-stop.R:74,120,153,183 (SMK_06A_cat4, SMK_09A_2003plus, *_complete_stub, *_daily_stub); smoke-start.R:102,163,304,546,592 (SMKG040_cat, SMKG203_cat, SMKG207_cat,
FIX: In v4, document worksheet-routed variables as roxygen data/topic pages (@name + NULL, like the existing variable docs in R/data.R) or pkgdown articles, not as exported functions. If a callable is genuinely wanted, make the stub delegate to rec_with_table(data, '<var>') instead of stopping. Remove the 18 stubs from NAMESPACE either way. [effort M]

### Three API generations coexist in one namespace with no deprecation markers, and the deleted v2 functions have no shims
WHY: Users cannot tell which of two (sometimes five) exported names for the same concept is current. v2 code calling bmi_fun() now fails with 'could not find function' despite the documented decision to ship shims. The worksheet itself — the package's central artifact — exposes the inconsistency, so even non-programmer users reading variable_details.csv see two naming conventions.
EVIDENCE: NAMESPACE grouping (125 exports): 43 calculate_*, 2 assess_*, 3 categorize_*, 3 score_*/adjust_*, 27 legacy *_fun (pack_years_fun, smoke_simple_fun, age_cat_fun, SMKDSTY_fun, EDUDR04_fun, ...), 18 get_*, 1 set_*, 6 CCHS-variable-named functions (DPSDPP, DPSDSF, GEN_02A2, LBFA_31A, LBFA_31A_a, LBFA_31A_b), plus engine/cache exports | Duplicate logic exported under both generations: R/smoking.R:266 pack_years_fun alongside R/smoke-pack-years.R:120 calculate_pack_years; smoke_simple_fun alongside c
FIX: Implement the already-decided R/deprecated-aliases.R: thin wrappers with .Deprecated() for every removed/renamed v2 function. Mark all remaining *_fun exports deprecated in roxygen (@keywords internal + lifecycle note) with a v4 removal date. Migrate worksheet Func:: references to the verb-prefixed names in one sweep so variable_details.csv shows a [effort M]

### Engine and cache internals leak into the public API (~24 exports), inflating the surface to 125 symbols
WHY: A 125-symbol surface for a harmonization package whose core workflow is one function plus ~30 DV functions makes the reference index unscannable and creates compatibility obligations for functions that were never designed as API (cache invalidation helpers, year-parsing heuristics). apply_database_heuristics and extract_years_from_database_names are implementation details of auto-detection; exporting them freezes internals the v4 refactor needs t
EVIDENCE: NAMESPACE exports engine helpers: is_equal (R/recode-with-table.R:33, a 3-line NA-safe '==' used internally), if_else2 (R/utility-functions.R:30, used 216 times in R/), merge_rec_data | NAMESPACE exports 15 missing-pattern-cache symbols: has_cached_pattern, get_cached_pattern, cache_pattern, clear_missing_patterns_cache, clear_complete_patterns_cache, get_missing_pattern, get_missing_pattern_auto, get_missing_pattern_bulk, get_complete_pattern, get_copy_mappings, get_else_mappings, get_variable_
FIX: Adopt the CEP-017 shortlist: keep clean_variables, any_missing, assign_missing, get_priority_missing, get_missing_pattern, get_complete_pattern, get_variables, get_variable_details and label utilities public; demote the rest of the cache/heuristic/discovery layer to @keywords internal (or an explicit cchsflow:::dev namespace) in v4 with one depreca [effort M]

### rec_with_table has 11 parameters; at least 4 are unused anywhere in the package's own vignettes/tests, and one (custom_function_path) sources arbitrary code with a comment that contradicts the code
WHY: The single front-door function carries four parameters with no demonstrated use, one parameter that does NSE guessing of a critical key (database_name), one that silently mutates the global environment, and one whose type changes its meaning. This is exactly the 'complex API' the recodeflow scoping complains about, and every extra parameter is a compatibility constraint on the planned v4 decomposition into detect/recode/derive/label stages.
EVIDENCE: /Users/dmanuel/github/cchsflow/R/recode-with-table.R:154-165 signature: data, variables, database_name, variable_details, else_value, append_to_data, log, notes, var_labels, custom_function_path, attach_data_name (11 params). recodeflow's copy has grown to 15 (/Users/dmanuel/github/recodeflow/R/recode-with-table.R:218-233 adds id_role_name, name_of_environment_to_load, append_non_db_columns, tables) | Call-site frequency across vignettes/ and tests/: log=21, var_labels=4, notes=3, attach_data_na
FIX: For v4: split rec_with_table into pipeline stages (per CEP-017 phase 5) and shrink the orchestrator to data / variables / database_name / variable_details plus an options object or ... for the long tail. Drop custom_function_path (users can source() themselves; if kept, evaluate in a child environment as the comment promises). Make database_name re [effort L]

### rec_with_table roxygen documents the wrong defaults for two of its parameters
WHY: The flagship function's help page misstates its own defaults in both directions. A user reading ?rec_with_table will expect appended output and silent notes and get the opposite. Documentation that contradicts code on the single most-used entry point erodes trust in all the other docs.
EVIDENCE: R/recode-with-table.R:113-114: '@param append_to_data Logical, if \code{TRUE} (default), recoded variables will be appended to the data.' vs R/recode-with-table.R:160: 'append_to_data = FALSE' | R/recode-with-table.R:117-118: '@param notes Logical, if \code{FALSE} (default), will not print the content inside the Note column' vs R/recode-with-table.R:162: 'notes = TRUE' | vignettes/get_started-ish text contradicts the roxygen too: a vignette states 'append_to_data defaults to FALSE' (grep hit 'vi
FIX: One-line roxygen fixes for both @param entries (and an audit of the remaining nine for drift), plus a testthat test that formals(rec_with_table) match the documented defaults to prevent regression. [effort S]

### Error/warning messages are base-R only and frequently unactionable, ungrammatical, or buggy; cli is in Depends but never used
WHY: The recodeflow scoping's top user complaint is 'Users not knowing why a function failure has taken place' (/tmp/recodeflow-pr43/index.qmd:36). The engine's failure messages omit which database/row/variable combination failed, contain typos and grammar errors that look unprofessional in console output, and in two places are functionally wrong (vectorized paste, wrong variable). Declaring cli in Depends while never calling it both bloats the depend
EVIDENCE: Counts: 91 stop() and 32 warning() calls in R/*.R; grep 'cli::|rlang::abort|cli_abort' R/*.R -> 0 hits; DESCRIPTION Depends includes cli (grep 'cli::' R/ -> 0 namespaced calls) | R/recode-with-table.R:750 stop("Invalid Argument was passed") — no argument name, no value, no call context | R/recode-with-table.R:297-300 stop("The passed labels was not a named vector please follow\n          the c(var_name = varLalbel) format") — typo 'varLalbel', embedded newline+indentation prints literally
FIX: Adopt cli::cli_abort()/cli_warn() (already a dependency) with rlang-style messages for v4: every engine error states variable, database, worksheet row, and a next action. Fix the feeder_vars/non_func_missing_variables bug and the two vectorized paste() sites now (S-effort bug fixes independent of v4). Add a style rule: no multi-line string literals [effort M]

### pkgdown reference index is stale: lists ~25 deleted v2 topics, omits all ~60 new v3 exports; vignettes still teach bmi_fun
WHY: pkgdown fails the build (or silently drops topics, depending on settings) when reference entries point to missing topics, so the v3 site either cannot build or publishes a reference index describing the v2 API. The tutorial vignettes teach a function and worksheet syntax that no longer exists in the package — the first thing a new v3 user follows is wrong.
EVIDENCE: _pkgdown.yml reference 'Derived variable functions' lists bmi_fun, adjusted_bmi_fun, bmi_fun_cat, pct_time_fun, pct_time_fun_cat, resp_condition_fun1/2/3, binge_drinker_fun, adl_fun, adl_score_5_fun, low_drink_short_fun, low_drink_long_fun, low_drink_score_fun, low_drink_score_fun1, immigration_fun (_pkgdown.yml:61-92) — none exist (man/bmi_fun.Rd, man/adl_fun.Rd, man/immigration_fun.Rd etc. deleted per git status; 0 NAMESPACE hits) | _pkgdown.yml 'Potentially problematic variables' lists ALW_1,
FIX: Regenerate _pkgdown.yml reference sections from the current NAMESPACE grouped by the v3 taxonomy (engine; DV functions by domain; worksheet tools; missing-data primitives; datasets), and sweep vignettes for deleted names (grep for the 25 removed topics). Add 'pkgdown::check_pkgdown()' to CI so reference/NAMESPACE drift fails fast. [effort S]

================================================================================
## data-artifacts — HIGH severity details

### Three competing runtime sources of worksheet metadata; one path writes RData into the installed package directory
WHY: The legacy engine (rec_with_table) and the v3 3-step infrastructure can read different physical artifacts in the same session: lazy data, CSV, or an auto-generated on-disk cache. Any of the three can drift. Writing into the package installation directory at runtime is a CRAN policy violation and breaks shared/read-only library installs. The use_rdata defaults are inconsistent across layers (TRUE in the loader, FALSE in the getters), so behaviour 
EVIDENCE: /Users/dmanuel/github/cchsflow/R/recode-with-table.R:170-178 — rec_with_table falls back to lazy data: data(variable_details, package = "cchsflow", envir = environment()) (the data/*.RData copies, 16-col projection) | /Users/dmanuel/github/cchsflow/R/worksheet-getters.R:48 and :212-214 — get_variables()/get_variable_details() default use_rdata = FALSE, reading inst/extdata/*.csv (23 cols) via load_worksheet_metadata() | /Users/dmanuel/github/cchsflow/R/worksheet-loaders.R:32 — load_worksheet_met
FIX: v4: one metadata accessor backed by a package-environment memoised cache (in-memory only). Declare one canonical on-disk artifact (the CSVs) and treat any RData as a build-time product. Delete save_metadata_to_rdata() and the RData-preference branch; remove the dead use_rdata plumbing. [effort M]

### No data-raw/ pipeline: data/*.RData rebuilt by hand with an undocumented 18->10 / 23->16 column projection
WHY: The lazy-data worksheets are a de facto public API (rec_with_table's default input, exported datasets with man pages, tested by tests/testthat/test-data.R), yet their derivation from the CSVs is tribal knowledge reproduced by hand each time. Any contributor who rebuilds with the wrong column set silently changes the package API. There is no CI check that data/*.RData matches the projection of inst/extdata/*.csv.
EVIDENCE: ls /Users/dmanuel/github/cchsflow/data-raw -> NO data-raw directory | grep across repo finds no usethis::use_data()/use_data() call anywhere; CONTRIBUTING.md contains no RData/rebuild guidance | Rebuilds appear only as ad hoc commit messages: d50d7aa2 'rebuilt RData from csv worksheets', e3fbf85f 'Cleaned csv worksheets ... rebuild RData as a result', f934ebf2 'rebuilt .RData files from updated csv worksheets'
FIX: Add data-raw/rebuild-worksheets.R that reads the CSVs, applies an explicit, commented column projection, and calls usethis::use_data(..., overwrite = TRUE); add a CI/testthat check asserting identical(projection(csv), rdata). Document in CONTRIBUTING.md. [effort S]

### Git history bloat: 844MB .git, ~420MB of it is 431 committed versions of variable_details.csv
WHY: A 1MB CSV that is rewritten on nearly every harmonization PR (often with Excel-induced whole-file reformatting — quoting churn, trailing empty columns, row re-sorting) behaves like an uncompressable binary in git. Clone time and CI checkout cost grow monotonically; an 844MB .git for a metadata package is embarrassing and will be inherited by v4 unless addressed. The committed pkgdown docs/ and once-committed DDI XMLs compound it.
EVIDENCE: du -sh .git -> 844M | Blob-size sweep by path: inst/ 525.9MB across 853 blobs; inst/extdata/variable_details.csv alone = 431 unique blobs, 420.4MB; inst/extdata/variables.csv = 325 blobs, 36.8MB | inst/extdata/CCHS_DDI/*.xml (since deleted, e.g. commit e15b1088/ea2afcca era, removed by 7f27a2ef '[Feat] Updated cran check') = 23 blobs, 51.6MB still in history
FIX: For v4, start a fresh repo (or git filter-repo the history) and stop committing build products: deploy pkgdown via GHA instead of docs/, never commit DDI/source documentation bundles, and adopt the canonical-CSV-with-minimal-quoting discipline (already enforced by exec/fix-worksheets.R) plus row-stable sorting to keep diffs delta-friendly. Consider [effort L]

### Shipped sample data covers none of the cycles v3 newly harmonizes (2019-2023)
WHY: The headline feature of v3 is extending harmonization through 2023, but no shipped artifact lets a user or CI run rec_with_table()/vignettes end-to-end on a 2019-2020, 2022, or 2023 PUMF. L6 implementation validation of new worksheet rows cannot be exercised inside the package. Master (_m) cycles are untestable by design (confidential), but the PUMF gaps are fixable. The malformed databaseStart tokens are silent dead rows.
EVIDENCE: variables.csv databaseStart references 34 distinct databases incl. cchs2019_2020_p, cchs2022_p, cchs2023_p, cchs2019_2020_m, cchs2021_m, cchs2022_m, cchs2023_m; data/ ships only 12 PUMF subsets ending at cchs2017_2018_p (file mtimes: 2022-02-22 and 2022-09-08; none newer) | Referenced-but-not-shipped list (Rscript check): cchs2019_2020_p, cchs2022_p, cchs2023_p plus all 16 _m master databases | DESCRIPTION Description still reads 'currently, 2001 to 2018' (Version: 2.1.0, Date: 2022-05-05)
FIX: Generate 200-row subsets of the 2019-2020, 2022, and 2023 PUMFs through the new data-raw pipeline (StatCan open licence permits redistribution, as for existing subsets); add them to data/ with proper Rd provenance. Add a worksheet check validating databaseStart tokens against an enumerated database registry. Document that _m cycles are validated on [effort M]

================================================================================
## dependencies-namespace — HIGH severity details

### All 9 runtime dependencies in Depends; no Imports field exists
WHY: R Packages 2e (Wickham & Bryan, ch. 'Dependencies') is explicit: 'Unless there is a good reason otherwise, you should always list packages in Imports not Depends.' Depends mutates the user's global search path as a side effect of library(cchsflow): a user who attaches cchsflow gets a different read_sas() (sjlabelled's, not haven's) and a different filter()/lag() (dplyr's, not stats') than before, with only easily-missed startup masking messages. 
EVIDENCE: DESCRIPTION lines 38-48: Depends: R (>= 3.5.0), haven (>= 1.1.2), dplyr (>= 0.8.2), sjlabelled (>= 1.0.17), stringr (>= 1.2.0), magrittr, yaml, readr, purrr, cli -- and no Imports: field anywhere in the file | Measured cost (Rscript on installed versions): haven 28 + dplyr 284 + sjlabelled 46 + stringr 63 + magrittr 42 + yaml 6 + readr 103 + purrr 184 + cli 240 = 996 exported symbols attached to the user's search path by library(cchsflow) | Masking measured: dplyr masks 6 base/stats names (filte
FIX: Move all 9 packages from Depends to a new Imports field, keeping only R (>= ...) in Depends. The package code needs almost nothing: the 3 unqualified calls in recode-with-table.R are already covered by importFrom. The real (small) cost is docs: Rd examples that assume attachment (e.g. unqualified filter() in worksheet-getters.R:209-210 example; 42  [effort M]

### Undeclared dependencies: glue and stats used via :: but absent from DESCRIPTION; withr used in tests but not in Suggests
WHY: CRAN policy and Writing R Extensions require every package whose namespace is accessed (even via ::) to be declared in Imports, and every package used only in tests/vignettes to be in Suggests. An undeclared Import means install.packages('cchsflow') can produce a library where check_worksheet() errors at runtime with 'there is no package called glue' -- the dependency solver never installed it. This is a hard CRAN blocker and exactly the class of
EVIDENCE: R/check-worksheet.R:274, 291, 313, 336, 359, 378, 396, 415 -- eight glue::glue() calls; 'glue' appears nowhere in DESCRIPTION | R/alcohol.R:93 and R/clean-variables.R:637 -- stats::setNames(); 'stats' not declared in Imports | tests/testthat/helper-create-test-csv.R:19 -- withr::local_tempfile(); Suggests lists only testthat, kableExtra, DT, rmarkdown, knitr, pkgdown
FIX: Add glue and stats to Imports (or, since the team standard is cli for messaging, replace the 8 glue::glue() calls with cli::format_inline()/sprintf and avoid the new dependency); add withr to Suggests. One-line DESCRIPTION edits plus optionally an 8-call mechanical refactor. [effort S]

### 125 exports with ~36 internal plumbing functions leaked into the public API
WHY: R Packages 2e: 'export a function only if it is part of the API you are committing to... the fewer exports, the easier it is to change things later.' Every exported symbol must be documented, kept backward-compatible, and survives into pkgdown and CRAN checks. Exporting the missing-pattern cache, database heuristics, and worksheet-loader internals freezes the exact implementation details the v4 engine refactor intends to replace -- v4 would eithe
EVIDENCE: NAMESPACE: 125 export() lines (grep -c). Cross-referencing inst/extdata/variable_details.csv shows only 46 are referenced as Func:: by the worksheets; 79 are not | 36 of the 79 are infrastructure: cache_pattern, get_cached_pattern, has_cached_pattern, clear_missing_patterns_cache, clear_complete_patterns_cache, get_complete_pattern, get_missing_pattern, get_missing_pattern_auto, get_missing_pattern_bulk, get_variable_missing_pattern, apply_database_heuristics, auto_detect_database, extract_years
FIX: Triage the 125 exports into three tiers before v4: (1) engine API (rec_with_table, merge_rec_data, set_data_labels, clean_variables, any_missing/get_priority_missing); (2) DV functions referenced by worksheets (46 Func:: targets, keep exported for transparency); (3) infrastructure (36) -- flip @export to @keywords internal or @noRd, or at minimum m [effort M]

================================================================================
## engine-internals — HIGH severity details

### List-mode recoding applies the first database's rules to every database (silent data corruption)
WHY: A documented API mode ('data ... Can also be a list of dataframes', doc lines 103-107) silently produces wrong harmonized data for every database after the first. This is exactly the class of silent cross-cycle error the package exists to prevent, and it is invisible unless the user notices grepl warnings.
EVIDENCE: /Users/dmanuel/github/cchsflow/R/recode-with-table.R:188-203 -- for (data_name in database_name) loop passes database_name = database_name (the FULL vector), not data_name, to recode_call | /Users/dmanuel/github/cchsflow/R/recode-with-table.R:320 -- grepl(database_name, variable_details$databaseStart) uses only pattern[1] when given a vector | Live repro (Rscript, devtools::load_all): list(db_one, db_two) with distinct rules (db_two recodes to 991/992) returned db_two$TEST_VAR == c('1','2') -- d
FIX: Pass data_name (the loop variable) to recode_call; also apply attach_data_name in this branch; add list-mode regression test with per-database rules. One-line fix plus tests in v3; in v4, make database identity an explicit per-call argument rather than a regex filter. [effort S]

### Interval parser silently mangles set-syntax recStart ('[7,8,9]' becomes interval [7,8], third value dropped to else)
WHY: The engine accepts-and-mangles worksheet syntax that authors evidently believed valid, with no validation layer; correctness currently depends on a coincidence in the else rows. A grammar the parser does not reject is a grammar it must implement.
EVIDENCE: /Users/dmanuel/github/cchsflow/R/recode-with-table.R:571-601 -- strsplit on ',' but only from_values[[1]] and [[2]] are used; third element ignored; interval_right becomes '8' -> invalid -> silently replaced by default '[,]' (lines 604-609) | Live repro: recStart '[7,8,9]' -> recEnd NA::b with else -> 2 produced SRC 7,8 -> 'NA(b)' but SRC 9 -> '2' (else), with runtime message 'invalid interval was passed. Default interval will be used: [,]' | Worksheet scan (Rscript read.csv on inst/extdata/vari
FIX: Define and validate a formal recStart grammar (single value | [a,b] with (/[ variants | else | else copy); error loudly on >2 comma elements or unparseable cells; fix the bracket-detection regex to '^\\[.*\\]$' style anchors. In v4 move validation into check-worksheets so bad syntax never reaches the engine. [effort M]

### Split missing-data regime: literal 'NA(b)' string sentinels for categoricals vs haven::tagged_na for continuous; factor coercion destroys tagged NAs
WHY: Two incompatible missingness encodings live in one output dataframe: is.na() is FALSE for categorical missing ('NA(b)' is a level), TRUE for continuous missing, and the tag detail v3 functions carefully produce is destroyed at the engine boundary for cat-typed deriveds. This directly blocks the v4 goal of a coherent metadata-driven missing-data contract and contaminates downstream tables/models.
EVIDENCE: /Users/dmanuel/github/cchsflow/R/recode-with-table.R:807-825 -- recode_variable_NA_formating: cat -> paste('NA(', tag, ')') string; cont -> haven::tagged_na(tag) | Live repro output: categorical TEST_VAR values are the literal string/factor level 'NA(b)', not NA | /Users/dmanuel/github/cchsflow/R/recode-with-table.R:949-954 -- derived cat variables coerced with as.factor(unlist(...)); live demo: as.factor(c(1,2,tagged_na('b'))) loses the 'b' tag (na_tag NA after round trip)
FIX: Pick one representation per output contract (haven labelled + tagged_na, or explicit factor missing levels) and apply it uniformly in one boundary function; never coerce through as.factor/as.character for values carrying tags. This is an architecture decision for the v4 engine, with a migration shim for v3. [effort L]

### Derived-variable dispatch: deprecated rowwise()+do() calling custom functions once per row via unnamed positional do.call
WHY: The dispatch relies on two deprecated dplyr idioms slated for eventual removal, has O(rows) interpretive overhead that dominates rec_with_table runtime for derived variables, and the unnamed positional contract is the single most dangerous coupling in the package: nothing checks that feeder order matches formals.
EVIDENCE: /Users/dmanuel/github/cchsflow/R/recode-with-table.R:937-947 -- recoded_data %>% rowwise() %>% select(used_feeder_vars) %>% do(column_being_added = calculate_custom_function_row_value(., ...)); do() superseded since dplyr 1.0.0 (2020); select() with a bare character vector triggers the tidyselect >=1.1 'use all_of()' deprecation | /Users/dmanuel/github/cchsflow/R/recode-with-table.R:974-983 -- row_values <- unname(row_values); do.call(get(custom_function_name), row_values): arguments matched pur
FIX: Replace with a single vectorized call: do.call(fun, setNames(as.list(recoded_data[used_feeder_vars]), names(formals(fun))[seq_along(used_feeder_vars)])) after asserting length/name compatibility against formals(); fall back to mapply only for genuinely scalar functions. v4 should require named argument binding declared in the worksheet or function  [effort L]

================================================================================
## labels-metadata-layer — HIGH severity details

### rec_with_table() fails on any tibble input inside label_data(); issue #159 misdiagnoses this as a haven_labelled problem
WHY: The engine's only entry point silently assumes base-data.frame single-bracket semantics deep inside the label layer. Every modern import path (haven::read_sav, readr::read_csv, ODESSI PUMF distributions) returns tibbles, so the package fails on the data sources it exists to serve, and the open issue's proposed fix targets the wrong cause. A v4 that keeps column-extraction-by-`[ , ]` will re-inherit the bug.
EVIDENCE: /Users/dmanuel/github/cchsflow/R/label-utils.R:184-211 - label_data uses single-bracket `data_to_label[, variable_name]` nine times; on a tibble this returns a 1-column tibble, so `as.numeric(data_to_label[, variable_name])` (line 203-204) throws "'list' object cannot be coerced to type 'double'" and `is.factor(...)` (lines 184, 198) is always FALSE | Live repro (labels-repro.R): tibble::as_tibble(cchs2001_p) with PLAIN numeric columns -> ERROR: 'list' object cannot be coerced to type 'double'; 
FIX: Short term: replace `data_to_label[, variable_name]` with `data_to_label[[variable_name]]` throughout label_data (and audit recode-with-table.R for the same pattern), add a tibble + haven_labelled regression test, and correct the diagnosis on issue #159. v4: operate on extracted vectors and reassign once per column; accept any data.frame subclass. [effort S]

### set_data_labels()/merge_rec_data() hard-stop on the package's own shipped worksheets (heterogeneous variableStartLabel)
WHY: The label layer treats a per-source metadata column (the StatCan label of the start variable, which legitimately varies by era/cycle) as if it were a single harmonized-variable label, then enforces that misconception with stop(). The crash fires after all recoding work is done, in a utility whose job is cosmetic, and it fires on metadata the package itself ships. It also imposes a hidden authoring constraint: worksheet authors cannot faithfully r
EVIDENCE: /Users/dmanuel/github/cchsflow/R/label-utils.R:132-148 - create_label_list_element stop()s if any row of a cat variable has a different variableStartLabel from row 1 | Live repro (labels-repro3.R): rec_with_table(cchs2015_2016_p, 'DHHGAGE_cat5') succeeds, but set_data_labels() on its output ERRORs: 'DHHGAGE_cat5 does not contain all identical variableStartLabel variable cant change variableLabel for different values. VAL1: Age (20-year age groups) VAL2: Age' | Worksheet scan (labels-repro2.R): 2
FIX: Short term: downgrade the stop() to a warning and take the first non-NA label (or the per-database row's label). Real fix (v4): source the harmonized variable label from variables.csv `label`/`labelLong` (one row per variable, no conflict possible) and reserve variableStartLabel as per-row provenance metadata - which also resolves the dead-column i [effort S]

### Categorical missing is a literal factor level 'NA(a)/NA(b)/NA(c)' while continuous missing is tagged_na - inconsistent semantics that break is.na(), block SPSS export, and leak into the v3 clean layer
WHY: Missingness is the analytically load-bearing metadata in CCHS work, and the package encodes it two different ways depending on toType. The categorical encoding ('NA(b)' as a valid factor level) silently enters models, crosstabs and survey weights as a real category, and defeats every standard missing-data tool (is.na, na.omit, tidyr::drop_na). The dual encoding forces string round-tripping inside the package itself (clean-variables.R) and makes f
EVIDENCE: /Users/dmanuel/github/cchsflow/R/recode-with-table.R:807-825 - recode_variable_NA_formating: cat type gets the string paste('NA(', tag, ')'), cont type gets haven::tagged_na(tag) | Live repro (labels-repro2.R): DHH_SEX with 5 NA source rows -> factor levels '1, 2, NA(b)'; table() shows NA(b)=5, <NA>=0; is.na(out$DHH_SEX[1]) is FALSE on a missing observation | Live repro (labels-repro.R): haven::write_sav() on rec_with_table output ERRORs: 'Failed to insert value [195, 2]: The file format does no
FIX: Pick one ecosystem-native representation in v4: haven_labelled (numeric codes + tagged_na for missing, value labels on both valid and missing codes), with labelled_spss/na_values or zap_missing() helpers for SPSS export, and an explicit opt-in as_factor() conversion for users who want factors. Keep 'NA(a)' strings only as a deprecated legacy output [effort L]

### Value labels are attached in a nonstandard sjlabelled-on-factor form (character-coded 'labels' attr) that labelled/haven cannot consume
WHY: The output is neither a useful factor (levels are opaque codes, not label text) nor a haven_labelled vector (class absent, codes stored as character). Every attribute except 'label' ('labels' on a factor with char codes, 'labels_long', 'label_long', 'unit') is a private convention no other package reads, so the metadata cannot flow into gtsummary, ggplot, codebook generators, or haven exports. This directly contradicts the v4 scoping commitment t
EVIDENCE: /Users/dmanuel/github/cchsflow/R/label-utils.R:183-196 - cat variables are coerced to factor of code strings, then sjlabelled::set_labels attaches labels; line 195-196 adds a custom 'labels_long' attribute; lines 207-212 add 'label', 'unit', 'label_long' attributes | Live repro (labels-repro.R, run 6): DHH_SEX output is class 'factor' with levels '1','2' and attr labels = c(Male='1', Female='2', 'not applicable'='NA(a)', missing='NA(b)') - typeof character, including codes ('NA(a)','NA(b)') that
FIX: v4: emit haven_labelled (numeric codes, named-numeric labels, var_label) as the default output; provide as_factor(levels='labels') for factor users; drop labels_long/label_long/unit attributes from columns and serve long labels and units through a codebook/metadata accessor (get_variable_metadata()) per the scoping docs' 'minimal coupling to data o [effort M]

================================================================================
## missing-data-architecture — HIGH severity details

### missing_priority_rules.yaml never shipped — built-in fallback silently inverts documented CCHS priority (NA::a wins instead of NA::b)
WHY: A documented, methodologically deliberate semantic (refusal is more informative than structural non-applicability) is silently replaced by its inverse on every installation, with only a session warning that reads like a config nit. Every derived variable that resolves competing missing tags via get_priority_missing() — all smoking, ADL, alcohol, BMI 3-step functions — currently labels conflicted rows 'not applicable' where the design says 'not st
EVIDENCE: find inst/metadata -type f — only documentation/metadata_registry.yaml, schemas/core/variable_details.yaml, schemas/core/variables.yaml exist; neither schemas/cchs/missing_priority_rules.yaml nor schemas/core/missing_priority_rules.yaml is present | /Users/dmanuel/github/cchsflow/R/missing-data-functions.R:168-172 — load_priority_rules() looks for both paths | /Users/dmanuel/github/cchsflow/R/missing-data-functions.R:194-198 — built-in fallback rules <- list(na_a = 1, na_b = 2) ('na_a higher pri
FIX: Ship the YAML (recover content from commit 370b673b on 3-step-tidyverse) into inst/metadata/schemas/cchs/ and core/, or per v4 open question 2 fold priorities into the declared NA-pattern schema. Whichever wins, make the built-in fallback match the documented CCHS order and add a regression test asserting get_priority_missing(tagged a, tagged b) re [effort S]

### Level 2A database-config-loader never migrated: exported get_missing_pattern_auto() always errors; auto_detect_database() permanently on hardcoded fallback
WHY: Like the priority YAML, half of a designed configuration layer was migrated and half was not. The result is a public API that throws 'could not find function' (a packaging embarrassment R CMD check cannot catch because the call is inside a function body), exists()-guards that make behaviour depend on what happens to be in scope, and database auto-selection that silently degrades to lexicographic ordering — which happens to work for CCHS names but
EVIDENCE: grep: load_database_config / auto_detect_database_type are called at /Users/dmanuel/github/cchsflow/R/missing-pattern-cache.R:451-465 (guarded by exists()) and :873 (UNguarded) but defined nowhere in R/ or tests/ | NAMESPACE:96 — export(get_missing_pattern_auto) | Empirical: get_missing_pattern_auto('HWTGBMI_der') → 'ERROR: could not find function "load_database_config"' — an exported function that can never succeed
FIX: Decide in v4 scope: either migrate/reimplement the config loader (recoverable from the 3-step-tidyverse branch) or delete the dead parameter surface — remove get_missing_pattern_auto(), the database_config parameters, and the exists() guards, and make database selection an explicit required argument (the bulk path already requires it). The v4 engin [effort S]

### No cache invalidation: worksheet edits mid-session yield mixed-version results; clear API is partial; user-supplied variable_details ignored by the 3-step stack; runtime RData writes shadow CSV edits
WHY: The architecture treats worksheet metadata as immutable but provides no mechanism to enforce or detect that assumption. cchsflow's own development workflow (this repo, this week) edits variable_details.csv constantly in live sessions; the result is silently inconsistent recodes that differ depending on call order. The fragmented clear API means even a user who knows about caching cannot reliably reset it. The user-worksheet bypass breaks rec_with
EVIDENCE: Cache inventory (all package-namespace environments, no worksheet fingerprint in keys): .missing_patterns_cache and .database_warnings_cache (/Users/dmanuel/github/cchsflow/R/missing-pattern-cache.R:26-27), .complete_patterns_cache (:884), .priority_rules_cache (R/missing-data-functions.R:151), .cchsflow_cache$pattern_warnings (R/clean-variables.R:14-15), .variable_warnings_cache (R/worksheet-getters.R:22-24) | Keys are (variable, database) only: cache_pattern (missing-pattern-cache.R:107-134), 
FIX: v4: one cache registry keyed by (worksheet fingerprint [mtime+size or xxhash], variable, database); load_worksheet_metadata memoizes the parsed worksheet under the same fingerprint and invalidates all dependent pattern entries when it changes; single exported clear_cchsflow_caches(); thread the worksheet object (or fingerprint) explicitly through g [effort M]

### extract_variable_name() call-stack hack never resolves real call shapes — every any_missing/get_priority_missing falls back to HWTGBMI_der, whose pattern is EMPTY
WHY: Non-standard evaluation to guess a metadata key from the caller's source text is fragile by construction (fails for $-access, pipes, arithmetic, renamed locals) and here it fails for 100% of in-package call sites, silently. The entire Level 4→5 'metadata integration' is inoperative; the system works only because of an undeclared contract that inputs are pre-cleaned. A function whose correctness depends on how its arguments are spelled at the call
EVIDENCE: /Users/dmanuel/github/cchsflow/R/missing-data-functions.R:218-237 — sys.call(-1) + as.character(call_info[[2]]) + hardcoded fallback return('HWTGBMI_der') | Every real call site uses cleaned$VAR shape: e.g. R/smoking-status.R:456-457,468,480; R/smoking-cessation.R:143-183; as.character(quote(cleaned$SMK_005)) is c('$','cleaned','SMK_005') (verified), so the length-3 vector makes `nchar(var_name) > 0 && ...` error in the && — swallowed by tryCatch → fallback | Empirical probes (mimicking any_miss
FIX: Delete the NSE hack. Two clean options for v4: (a) make the contract explicit — any_missing()/get_priority_missing() operate only on tagged/plain NA (document that inputs must come from clean_variables()) and drop code-based detection entirely; or (b) accept an explicit pattern/variable_name argument (any_missing(..., .pattern = pat)) resolved once [effort M]

### Three NA representations in one rec_with_table() output: tagged_na (recoded cont), 'NA(a)'/'NA(b)' factor levels (recoded cat), plain NA (derived cat)
WHY: Missing-data semantics is the package's core value proposition, yet what a missing value looks like depends on which of three internal code paths produced the column. Analysts must know implementation details to count missing correctly, and the derived-categorical path destroys information (a/b distinction) that both other paths preserve. This blocks the v4 goal of consistent end-to-end semantics and is the kind of inconsistency that surfaces in 
EVIDENCE: /Users/dmanuel/github/cchsflow/R/recode-with-table.R:807-825 — recode_variable_NA_formating(): cat type → paste0('NA(', tag, ')') string (becomes a factor LEVEL); non-cat → haven::tagged_na(tag) | /Users/dmanuel/github/cchsflow/R/recode-with-table.R:949-954 — derived-variable dispatch: toType cat → as.factor(unlist(...)), which converts tagged NA doubles to plain NA (tag unrecoverable); verified at unit level: as.factor(unlist(list(1,2,tagged_na('b')))) → levels 1,2 + 1 plain NA | End-to-end, on
FIX: Standardize on haven semantics at the engine boundary: keep categoricals as haven::labelled doubles (value labels for categories, tagged_na for missing) through the whole pipeline, and convert to factor only at an explicit user-facing step (e.g. haven::as_factor(levels='labels') with an opt-in 'NA(x)' level shim for v3 compatibility). Derived dispa [effort L]
