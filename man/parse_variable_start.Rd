% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variable-tools.R
\name{parse_variable_start}
\alias{parse_variable_start}
\title{Parse variableStart field to extract source variable name}
\usage{
parse_variable_start(variable_start, database)
}
\arguments{
\item{variable_start}{Character string from the variableStart field.}

\item{database}{Character string specifying the target database
(e.g., \code{"cchs2001_p"}).}
}
\value{
Character string with the source variable name, or NULL. NULL means
  one of three things: (1) the variable is derived (\code{DerivedVar::}
  format — use \code{resolve_dependencies()} instead); (2) the variable is
  not available for the specified database (no matching row in
  variable_details.csv); or (3) the variableStart uses a \code{Func::}
  transformation marker.
}
\description{
Parses the \code{variableStart} field from variable_details.csv and extracts
the source variable name for a specific database. Supports all recodeflow
variableStart formats.
}
\details{
Supports four variableStart formats:
\enumerate{
  \item \strong{Database-prefixed}: \code{"cchs2001_p::RACA_6A"} — database-specific name
  \item \strong{Bracket (default)}: \code{"[ADL_01]"} — same name for all databases
  \item \strong{Mixed}: \code{"cchs2001_p::var1, [var2]"} — bracket is fallback default
  \item \strong{Plain}: \code{"varname"} — uses value as-is
}

Returns NULL for DerivedVar and Func formats (these require dependency
resolution, not simple name extraction).

\strong{Note on Master-only variables}: Variables available only in Master
files (e.g., \code{SMK_06C}, \code{SMK_09C}) have no rows in
variable_details.csv for PUMF databases. Calling this function with a PUMF
database will return NULL — this is correct behaviour, not an error.
}
\examples{
\dontrun{
# Database-prefixed
parse_variable_start("cchs2001_p::RACA_6A, cchs2003_p::RACA_6A1", "cchs2001_p")
# Returns: "RACA_6A"

# Bracket format (default for all databases)
parse_variable_start("[ADL_01]", "cchs2001_p")
# Returns: "ADL_01"

# Mixed — bracket is default for unlisted databases
parse_variable_start("cchs2001_p::AMSDMVA1, [AMMDMVA1]", "cchs2005_p")
# Returns: "AMMDMVA1"

# DerivedVar returns NULL
parse_variable_start("DerivedVar::[HWTGHTM, HWTGWTK]", "cchs2001_p")
# Returns: NULL
}
}
