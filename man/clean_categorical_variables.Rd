% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing-data-helpers.R
\name{clean_categorical_variables}
\alias{clean_categorical_variables}
\title{Clean and validate multiple categorical variables with comprehensive preprocessing}
\usage{
clean_categorical_variables(
  ...,
  valid_values,
  pattern_type = "single_digit_missing",
  log_level = "silent"
)
}
\arguments{
\item{...}{Named categorical variables to clean (e.g., sex = DHH_SEX, smoker_type = SMK_005)}

\item{valid_values}{Named list of valid values (e.g., list(sex = c(1, 2), smoker_type = c(1, 2, 3)))}

\item{pattern_type}{CCHS missing code pattern to use (default "single_digit_missing")}

\item{log_level}{Logging level: "silent" (default), "warning", "verbose"}

\item{skip_validation}{Logical. Skip parameter/length validation if already done upstream (default FALSE)}
}
\value{
Named list with cleaned variables (e.g., list(sex_clean, smoker_type_clean))
}
\description{
Universal preprocessing function that handles parameter validation, CCHS missing
code preprocessing, length compatibility, and valid values validation for any number of
categorical variables. Used across all derived variable domains.
}
\note{
Internal v3.0.0, last updated: 2025-06-30, status: active
}
\examples{
# Smoking status usage
cleaned <- clean_categorical_variables(
  smoker_type = SMK_005, ever_daily = SMK_030,
  valid_values = list(smoker_type = c(1, 2, 3), ever_daily = c(1, 2)),
  pattern_type = "single_digit_missing"
)

# Sex and age category usage
cleaned <- clean_categorical_variables(
  sex = DHH_SEX, age_started_cat = SMKG203,
  valid_values = list(sex = c(1, 2), age_started_cat = 1:11),
  pattern_type = "double_digit_missing"
)
}
\keyword{internal}
