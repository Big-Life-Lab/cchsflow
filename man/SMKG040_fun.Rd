% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smoking.R
\name{SMKG040_fun}
\alias{SMKG040_fun}
\title{Age started smoking daily - daily/former daily smokers (unified approach)}
\usage{
SMKG040_fun(
  SMKG203_cont,
  SMKG207_cont,
  validate_params = FALSE,
  min_SMKG203_cont = 8,
  max_SMKG203_cont = 84,
  min_SMKG207_cont = 8,
  max_SMKG207_cont = 84
)
}
\arguments{
\item{SMKG203_cont}{age started smoking daily (daily smokers)}

\item{SMKG207_cont}{age started smoking daily (former daily smokers)}

\item{validate_params}{logical, enable parameter validation (default FALSE)}

\item{min_SMKG203_cont}{minimum valid age for daily smokers (default 5)}

\item{max_SMKG203_cont}{maximum valid age for daily smokers (default 84)}

\item{min_SMKG207_cont}{minimum valid age for former daily (default 5)}

\item{max_SMKG207_cont}{maximum valid age for former daily (default 80)}
}
\value{
value for age started smoking daily for daily/former daily smokers
}
\description{
This function creates a continuous derived variable
(SMKG040_fun) that calculates the approximate age that a daily or former
daily smoker began smoking daily.
}
\examples{
# Standard CCHS processing
SMKG040_fun(c(18, NA), c(NA, 25))  # Returns c(18, 25)

# With validation for research data quality
SMKG040_fun(c(18, 4), c(NA, 25), validate_params = TRUE)  # Returns c(18, tagged_na("b"))

# Copy-paste standalone usage
library(dplyr); library(haven)
SMKG040_fun(18, NA)  # Works independently

}
