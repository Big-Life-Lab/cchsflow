% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing-data-helpers.R
\name{clean_for_categorization}
\alias{clean_for_categorization}
\title{Clean and validate values for categorization with vector support}
\usage{
clean_for_categorization(
  values,
  pattern_type = "triple_digit_missing",
  log_level = "silent"
)
}
\arguments{
\item{values}{Values to clean (single value, vector, numeric, character, or tagged_na)}

\item{pattern_type}{CCHS missing code pattern to use (default "triple_digit_missing")}

\item{log_level}{Logging level: "silent" (default), "warning", "verbose"}
}
\value{
Cleaned values ready for case_when() logic with proper tagged NA handling
}
\description{
Universal preprocessing function for categorization that handles both single values
and vectors. Provides comprehensive preprocessing, tagged NA handling, and prepares
values for use in dplyr::case_when() categorization logic. Used across all
derived variable categorization functions.
}
\note{
Internal v3.0.0, last updated: 2025-07-04, status: active - VECTOR AWARE
}
\examples{
# BMI categorization usage (vector)
clean_bmi <- clean_for_categorization(c(17, 22, 27, 997), "triple_digit_missing")

# Smoking categorization usage (single)
clean_status <- clean_for_categorization(smoking_status, "single_digit_missing")

# Integration with case_when
result <- dplyr::case_when(
  clean_bmi < 18.5 ~ "Underweight",
  clean_bmi >= 18.5 & clean_bmi < 25.0 ~ "Normal weight",
  # ... tagged NAs are handled automatically by preprocessing
)
}
\keyword{internal}
