% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-worksheet.R
\name{check_invalid_databases}
\alias{check_invalid_databases}
\title{Check variable_details.csv and variables.csv for invalid database names}
\usage{
check_invalid_databases(
  file_path,
  file_type = c("variables", "variable_details")
)
}
\arguments{
\item{file_path}{Path to variables.csv or variable_details.csv}

\item{file_type}{Type of file being checked. Either "variables" or
"variable_details".}
}
\value{
A list of errors found. Each error is a named list containing
information about the error.
}
\description{
Scans all `databaseStart` fields for database names that are known to be
invalid or deprecated. This catches errors that the recode block check
cannot detect because they are about the database name itself, not about
the recode structure.
}
\details{
Checks for:
\itemize{
  \item `_s` suffix — deprecated share files (use `_m` instead)
  \item `_i` suffix — deprecated ICES databases (use `_m` instead)
  \item `chs2` prefix without leading `c` — typo (should be `cchs2`)
  \item `cchs20013_` — extra zero typo (should be `cchs2013_`)
  \item Invalid PUMF databases: `cchs2021_p`, `cchs2022_p`, `cchs2023_p`
    — the 2021 CCHS was released as a combined 2021-2022 PUMF; 2022+ smoking
    variables were restructured into CSS/SPU modules with no standalone PUMF
}
}
\examples{
\dontrun{
check_invalid_databases("inst/extdata/variable_details.csv",
                        "variable_details")
}
}
