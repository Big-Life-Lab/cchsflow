% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variable-tools.R
\name{resolve_dependencies}
\alias{resolve_dependencies}
\title{Resolve the full dependency graph for target variables}
\usage{
resolve_dependencies(
  target_variables,
  variable_details = NULL,
  databases = NULL
)
}
\arguments{
\item{target_variables}{Character vector of cchsflow variable names.}

\item{variable_details}{Data frame from variable_details.csv. If NULL,
loads from package or repo.}

\item{databases}{Character vector. Optional — restrict dependency resolution
  to specific databases (e.g., \code{"cchs2001_p"}). If NULL, uses the union
  of all feeders across all databases.

  \strong{Known limitation}: When DerivedVar rows in variable_details.csv
  list both PUMF (\code{_p}) and Master (\code{_m}) databases in the same
  row with different feeder sets (e.g., \code{pack_years_der} uses
  \code{DHHGAGE_cont} for some cycles and \code{DHH_AGE} for others, but
  within rows that mix \code{_p} and \code{_m} databases), filtering to a
  single database will return the union of feeders from all matching rows.
  This means the resolved chain may include feeders that apply only to the
  other database type. This is a worksheet structure issue — the rows should
  be split by PUMF vs Master where feeder sets differ.}
}
\value{
A list with components:
  \describe{
    \item{ordered}{Character vector of variable names in topological order
      (leaves first, targets last).}
    \item{graph}{Named list. Each element is a list with:
      \code{feeders} (character vector of direct dependencies),
      \code{is_derived} (logical),
      \code{func} (character or NA — the Func:: function name if derived).}
    \item{warnings}{Character vector of any issues (missing variables,
      circular dependencies, feeders not found in worksheets).}
  }
}
\description{
Given one or more cchsflow variable names, returns the complete set of
variables needed (including transitive dependencies from DerivedVar chains),
in topological order from leaves to roots.
}
\examples{
\dontrun{
# Resolve all dependencies for pack_years_der
deps <- resolve_dependencies("pack_years_der")
deps$ordered
# [1] "SMKDSTY_A" "DHHGAGE_cont" "SMK_204" ... "pack_years_der"

# Check what a specific variable needs
deps$graph[["pack_years_der"]]$feeders
}
}
