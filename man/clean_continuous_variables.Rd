% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing-data-helpers.R
\name{clean_continuous_variables}
\alias{clean_continuous_variables}
\title{Clean and validate multiple continuous variables with comprehensive preprocessing}
\usage{
clean_continuous_variables(..., min_values, max_values, log_level = "silent")
}
\arguments{
\item{...}{Named continuous variables to clean (e.g., height = HWTGHTM, weight = HWTGWTK)}

\item{min_values}{Named list of minimum values (e.g., list(height = 0.914, weight = 27.0))}

\item{max_values}{Named list of maximum values (e.g., list(height = 2.134, weight = 135.0))}

\item{log_level}{Logging level: "silent" (default), "warning", "verbose"}

\item{skip_validation}{Logical. Skip parameter/length validation if already done upstream (default FALSE)}
}
\value{
Named list with cleaned variables (e.g., list(height_clean, weight_clean))
}
\description{
Universal preprocessing function that handles parameter validation, CCHS missing
code preprocessing, length compatibility, and bounds validation for any number of
continuous variables. Used across all derived variable domains.
}
\note{
Internal v3.0.0, last updated: 2025-06-30, status: active
}
\examples{
# BMI usage
cleaned <- clean_continuous_variables(
  height = HWTGHTM, weight = HWTGWTK,
  min_values = list(height = 0.914, weight = 27.0),
  max_values = list(height = 2.134, weight = 135.0)
)

# Smoking usage
cleaned <- clean_continuous_variables(
  age_started = SMKG203_cont,
  min_values = list(age_started = 8),
  max_values = list(age_started = 95)
)
}
\keyword{internal}
