% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing-data-helpers.R
\name{clean_single_value}
\alias{clean_single_value}
\title{Clean and validate a single value with comprehensive preprocessing}
\usage{
clean_single_value(
  value,
  pattern_type = "triple_digit_missing",
  log_level = "silent"
)
}
\arguments{
\item{value}{Single value to clean (numeric, character, or tagged_na)}

\item{pattern_type}{CCHS missing code pattern to use (default "triple_digit_missing")}

\item{log_level}{Logging level: "silent" (default), "warning", "verbose"}
}
\value{
Cleaned single value with appropriate tagged NA handling
}
\description{
Lightweight preprocessing function for single values (e.g., already calculated
BMI, ADL scores, alcohol consumption values). Handles missing data, CCHS missing
code preprocessing, and tagged NA detection. Used by categorization functions
across all derived variable domains.
}
\note{
Internal v3.0.0, last updated: 2025-06-30, status: active
}
\examples{
# BMI categorization usage
clean_bmi <- clean_single_value(bmi_value, "triple_digit_missing")

# Smoking categorization usage
clean_status <- clean_single_value(smoking_status, "single_digit_missing")

# ADL categorization usage
clean_score <- clean_single_value(adl_score, "triple_digit_missing")
}
\keyword{internal}
