% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing-data-helpers.R
\name{generate_tagged_na_conditions}
\alias{generate_tagged_na_conditions}
\title{Generate standardized tagged NA case_when conditions}
\usage{
generate_tagged_na_conditions(
  var,
  categorical_labels = FALSE,
  include_bounds = NULL
)
}
\arguments{
\item{var}{Variable to generate conditions for}

\item{categorical_labels}{Return string labels ("NA(a)") vs tagged_na objects (default FALSE)}

\item{include_bounds}{Optional bounds checking condition to insert before default}
}
\value{
List of case_when conditions in priority order
}
\description{
Creates reusable case_when conditions for handling tagged NAs in a consistent
priority order across all derived variable functions. Eliminates code duplication
of tagged NA handling patterns.
}
\note{
Internal v3.0.0, last updated: 2025-07-04, status: active
}
\examples{
# Usage in categorization functions
dplyr::case_when(
  !!!generate_tagged_na_conditions(clean_bmi, categorical_labels = TRUE),
  clean_bmi < 18.5 ~ "Underweight",
  clean_bmi >= 18.5 & clean_bmi < 25.0 ~ "Normal weight",
  .default = "NA(b)"
)

# Usage in validation functions
dplyr::case_when(
  !!!generate_tagged_na_conditions(var, include_bounds = var < min_val | var > max_val),
  TRUE ~ var
)
}
\keyword{internal}
